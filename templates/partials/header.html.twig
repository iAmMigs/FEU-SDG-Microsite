<header class="bg-white shadow-sm sticky top-0 z-50">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between items-center h-20">
            
            <div class="flex-shrink-0 flex items-center">
                <a href="{{ path('app_home') }}" class="flex items-center gap-3 group">
                       <img class="h-12 w-auto" src="{{ asset('images/Tech_Logo.png') }}" alt="FEU Logo"> 
                    <div class="flex flex-col">
                        <span class="font-black text-xl text-green-900 leading-tight group-hover:text-green-700 transition">FEU Institute of Technology</span>
                        <span class="text-[0.65rem] text-gray-500 font-bold tracking-widest uppercase">SDG Microsite</span>
                    </div>
                </a>
            </div>
            
            <nav class="hidden md:flex space-x-8 items-center">
                <a href="{{ path('app_home') }}" class="text-green-800 font-bold hover:text-yellow-500 transition px-2 py-2 text-sm">Home</a>
                <a href="#goals" class="text-gray-600 hover:text-green-800 transition px-2 py-2 text-sm font-medium">The SDGs</a>
                <a href="{{ path('app_about') }}" class="text-gray-600 hover:text-green-800 transition px-2 py-2 text-sm font-medium">About</a>
                <a href="{{ path('app_trending') }}" class="text-gray-600 hover:text-green-800 transition px-2 py-2 text-sm font-medium">Trending Studies</a>
                <a href="{{ path('app_news') }}" class="text-gray-600 hover:text-green-800 transition px-2 py-2 text-sm font-medium">News</a>
            </nav>

            <div class="flex items-center space-x-4">
                <a href="{{ path('app_library') }}" class="hidden md:inline-flex items-center justify-center px-5 py-2.5 text-sm font-bold rounded-full text-green-950 bg-[#FCC30B] hover:bg-yellow-400 transition shadow-sm">
                    Browse the Library
                </a>
                
                <div class="flex items-center md:hidden">
                    <button type="button" class="text-gray-500 hover:text-green-800 focus:outline-none p-2" aria-expanded="false">
                        <span class="sr-only">Open main menu</span>
                        <svg class="h-6 w-6" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
                        </svg>
                    </button>
                </div>
            </div>

        </div>
    </div>
</header>